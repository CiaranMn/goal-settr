<h1>Browse categories</h1>
<div class="container">

  <%@categories.each do |category|%>
  <li class="list-group-item d-flex justify-content-between align-items-center">
    <%=link_to category.name, category%>
    <span class="badge badge-primary badge-pill"><%=category.goals.count%></span>
  </li>
  <%end%>
</ul>

</div>
<!-- <h1>Top categories</h1>
<div class="stacked">
  <ul class="list-group">
  <table class="categories-leaderboard">
    <th><%= link_to "Category", category_index_url({q: :name}) %></th>
    <th><%= link_to "# of goals", category_index_url({q: :num_goals}) %></th>

  <% @categories.each do |category| %>
    <tr>
      <td><%= category.name%></td>
      <td>  <span class="badge badge-primary badge-pill"><%= category.num_goals%></span></td>
    </a></tr>
  <% end %>
</div> -->
